# Audio Sample Notes

Note that the Orpheus samples are weaker primarily because the model seems to need more epochs of training. Probably the model would also benefit from being trained on 60s audio samples rather than 30 seconds, especially if training on 2048 length (which is necessary to be able to do voice cloning with Orpheus).