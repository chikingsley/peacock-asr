[project]
name = "voxtral-modal-serving"
version = "0.1.0"
description = "Voxtral STT serving via vLLM on Modal"
requires-python = ">=3.11"
dependencies = [
    "modal",
    "python-dotenv",
    "huggingface-hub",
    "openai",
    "httpx",
]
