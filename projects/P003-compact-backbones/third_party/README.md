# P003 Third-Party

Store vendored upstream repos or pinned reference code here when `P003`
actually depends on them.

Examples:

- a pinned HMamba checkout
- a pinned NeMo/Citrinet reference tree if we decide to adapt Citrinet
