\section{Experimental Setup}

The final version of this section should consolidate:

\begin{enumerate}
  \item dataset and split details,
  \item backend descriptions,
  \item scorer hyperparameters,
  \item seed protocol,
  \item hardware and runtime provenance,
  \item primary metrics.
\end{enumerate}

The campaign outputs already live under
\texttt{projects/P001-gop-baselines/experiments/final/results/}. Those manifests
should be treated as the canonical numeric source when tables are populated.

This section should also include a compact figure showing the end-to-end
pipeline and a table describing the phase-1 and phase-2 experiment matrix.
