name: p001-paper-close-phase2-xlsr-b2-logit-margin
entity: peacockery
project: peacock-asr-p001-gop-baselines
method: grid
metric:
  name: pcc
  goal: maximize

command:
  - ${env}
  - PEACOCK_WANDB_GROUP=p001-paper-close-phase2-xlsr-b2-logit-margin
  - PEACOCK_WANDB_TRACK=track05
  - PEACOCK_WANDB_PROJECT_ID=P001
  - PEACOCK_WANDB_PHASE=phase2
  - PEACOCK_WANDB_JOB_ID=b2
  - PEACOCK_WANDB_RUN_PREFIX=p001-paper-close
  - PEACOCK_WANDB_JOB_TYPE=eval
  - PEACOCK_WANDB_TAGS=p001,paper-close,phase2,xlsr,b2,logit_margin
  - uv
  - run
  - --project
  - projects/P001-gop-baselines
  - peacock-asr
  - run
  - --backend=xlsr-espeak
  - --score-variant=logit_margin
  - --score-alpha=0.5
  - ${args}

parameters:
  seed:
    values: [501]
