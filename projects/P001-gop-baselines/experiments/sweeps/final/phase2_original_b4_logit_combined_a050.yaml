name: p001-paper-close-phase2-original-b4-logit-combined-a050
entity: peacockery
project: peacock-asr-p001-gop-baselines
method: grid
metric:
  name: pcc
  goal: maximize

command:
  - ${env}
  - PEACOCK_WANDB_GROUP=p001-paper-close-phase2-original-b4-logit-combined-a050
  - PEACOCK_WANDB_TRACK=track05
  - PEACOCK_WANDB_PROJECT_ID=P001
  - PEACOCK_WANDB_PHASE=phase2
  - PEACOCK_WANDB_JOB_ID=b4
  - PEACOCK_WANDB_RUN_PREFIX=p001-paper-close
  - PEACOCK_WANDB_JOB_TYPE=eval
  - PEACOCK_WANDB_TAGS=p001,paper-close,phase2,original,b4,logit_combined,alpha050
  - uv
  - run
  - --project
  - projects/P001-gop-baselines
  - peacock-asr
  - run
  - --backend=original
  - --score-variant=logit_combined
  - --score-alpha=0.50
  - ${args}

parameters:
  seed:
    values: [501]
